<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kaitka.vishal.meeta.purple_ecommerce">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Purple">
        <activity android:name=".Activites.MyAddressesActivity"></activity>
        <activity android:name=".Activites.AddAddressActivity" />
        <activity android:name=".Activites.DeliveryActivity" />
        <activity android:name=".Activites.OrderDetailsActivity" />
        <activity android:name=".Activites.ProductDetailsActivity" />
        <activity android:name=".Activites.CategoryActivity" />
        <activity
            android:name=".Activites.MainActivity"
            android:label="@string/title_activity_main"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Purple.NoActionBar" />
        <activity
            android:name=".Activites.RegisterActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".Activites.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>